version: 2
project_name: goreleaser-cross-toolchains
release:
  github:
    owner: goreleaser
    name: goreleaser-cross-toolchains
  tag: '{{ .PrefixedTag }}'
  name_template: '{{ .PrefixedTag }}'
builds:
  - id: goreleaser-cross-toolchains
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - "386"
    goamd64:
      - v1
    go386:
      - sse2
    goarm:
      - "6"
    goarm64:
      - v8.0
    gomips:
      - hardfloat
    goppc64:
      - power8
    goriscv64:
      - rva20u64
    targets:
      - linux_amd64_v1
      - linux_arm64_v8.0
      - linux_386_sse2
      - darwin_amd64_v1
      - darwin_arm64_v8.0
      - windows_amd64_v1
      - windows_arm64_v8.0
      - windows_386_sse2
    dir: .
    main: .
    binary: goreleaser-cross-toolchains
    builder: go
    skip: "true"
    tool: go
    command: build
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}} -X main.builtBy=goreleaser
archives:
  - id: default
    builds_info:
      mode: 493
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ with .Arm }}v{{ . }}{{ end }}{{ with .Mips }}_{{ . }}{{ end }}{{ if not (eq .Amd64 "v1") }}{{ .Amd64 }}{{ end }}'
    formats:
      - tar.gz
    files:
      - src: license*
      - src: LICENSE*
      - src: readme*
      - src: README*
      - src: changelog*
      - src: CHANGELOG*
srpm:
  nfpmrpm: {}
  package_name: goreleaser-cross-toolchains
  file_name_template: '{{ .PackageName }}-{{ .Version }}.src.rpm'
  bins:
    goreleaser-cross-toolchains: '%{goipath}'
snapshot:
  version_template: '{{ .Version }}-SNAPSHOT-{{ .ShortCommit }}'
checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_checksums.txt'
  algorithm: sha256
dockers_v2:
  - id: toolchains
    dockerfile: Dockerfile
    images:
      - goreleaser/goreleaser-cross-toolchains
      - ghcr.io/goreleaser/goreleaser-cross-toolchains
    tags:
      - '{{ .Tag }}'
      - v{{ .Major }}.{{ .Minor }}
      - latest
    extra_files:
      - ubuntu.sources
    platforms:
      - linux/amd64
      - linux/arm64
    build_args:
      CROSSBUILD_ARCH: '{{ trim (trimprefix (trimsuffix .Env.CROSSBUILD_ARCH "\"") "\"") }}'
      DEBIAN_FRONTEND: '{{ .Env.DEBIAN_FRONTEND }}'
      DPKG_ARCH: '{{ trim (trimprefix (trimsuffix .Env.DPKG_ARCH "\"") "\"") }}'
      OSXCROSS_VERSION: '{{ .Env.OSXCROSS_VERSION }}'
    retry:
      attempts: 10
      delay: 10s
      max_delay: 5m0s
    sbom: "true"
  - id: toolchains-musl
    dockerfile: musl.Dockerfile
    images:
      - goreleaser/goreleaser-cross-toolchains
      - ghcr.io/goreleaser/goreleaser-cross-toolchains
    tags:
      - '{{ .Tag }}-musl'
      - v{{ .Major }}.{{ .Minor }}-musl
      - latest-musl
    extra_files:
      - cross-make
    platforms:
      - linux/amd64
      - linux/arm64
    build_args:
      MUSL_TARGETS: '{{ trim (trimprefix (trimsuffix .Env.MUSL_TARGETS "\"") "\"") }}'
      OSXCROSS_VERSION: '{{ .Env.OSXCROSS_VERSION }}'
    retry:
      attempts: 10
      delay: 10s
      max_delay: 5m0s
    sbom: "true"
docker_digest:
  name_template: digests.txt
changelog:
  filters:
    exclude:
      - '^docs:'
      - '^test:'
  sort: asc
  use: github
  format: '{{ .SHA }}: {{ .Message }} ({{ with .AuthorUsername }}@{{ . }}{{ else }}{{ .AuthorName }} <{{ .AuthorEmail }}>{{ end }})'
  title: Changelog
dist: dist
docker_signs:
  - id: default
    cmd: cosign
    args:
      - sign
      - ${artifact}
      - --yes
    output: "true"
env_files:
  github_token: ~/.config/goreleaser/github_token
  gitlab_token: ~/.config/goreleaser/gitlab_token
  gitea_token: ~/.config/goreleaser/gitea_token
source:
  name_template: '{{ .ProjectName }}-{{ .Version }}'
  format: tar.gz
gomod:
  gobinary: go
announce:
  twitter:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
  mastodon:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
    server: ""
  reddit:
    title_template: '{{ .ProjectName }} {{ .Tag }} is out!'
    url_template: '{{ .ReleaseURL }}'
  slack:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
    username: GoReleaser
  discord:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
    author: GoReleaser
    color: "3888754"
    icon_url: https://goreleaser.com/static/avatar.png
  teams:
    title_template: '{{ .ProjectName }} {{ .Tag }} is out!'
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
    color: '#2D313E'
    icon_url: https://goreleaser.com/static/avatar.png
  smtp:
    subject_template: '{{ .ProjectName }} {{ .Tag }} is out!'
    body_template: 'You can view details from: {{ .ReleaseURL }}'
  mattermost:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
    title_template: '{{ .ProjectName }} {{ .Tag }} is out!'
    username: GoReleaser
  linkedin:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
  telegram:
    message_template: '{{ print .ProjectName " " .Tag " is out! Check it out at " .ReleaseURL | mdv2escape }}'
    parse_mode: MarkdownV2
  webhook:
    message_template: '{ "message": "{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}"}'
    content_type: application/json; charset=utf-8
    expected_status_codes:
      - 200
      - 201
      - 202
      - 204
  opencollective:
    title_template: '{{ .Tag }}'
    message_template: '{{ .ProjectName }} {{ .Tag }} is out!<br/>Check it out at <a href="{{ .ReleaseURL }}">{{ .ReleaseURL }}</a>'
  bluesky:
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
  discourse:
    title_template: '{{ .ProjectName }} {{ .Tag }} is out!'
    message_template: '{{ .ProjectName }} {{ .Tag }} is out! Check it out at {{ .ReleaseURL }}'
    username: system
git:
  tag_sort: -version:refname
mcp:
  name: ""
  title: ""
  auth:
    type: none
github_urls:
  download: https://github.com
gitlab_urls:
  download: https://gitlab.com
partial:
  by: goos
nightly:
  version_template: '{{ incpatch .Version }}-{{ .ShortCommit }}-nightly'
  tag_name: nightly
